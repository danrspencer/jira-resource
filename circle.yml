machine:
  node:
    version: node
test:
  post:
    - mocha test --reporter mocha-junit-reporter:
        environment:
          MOCHA_FILE: $CIRCLE_TEST_REPORTS/junit/test-results.xml
    - istanbul cover --report clover --dir $CIRCLE_TEST_REPORTS/coverage src/*.js
dependencies:
  cache_directories:
    - "node_modules"
